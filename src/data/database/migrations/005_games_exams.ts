import type { Migration } from './index';

/** Exams, games, challenges and their attempt history. */
export const migration005: Migration = {
  version: 5,
  name: 'games_exams',
  statements: [
    `CREATE TABLE IF NOT EXISTS exams (
      id TEXT PRIMARY KEY,
      code TEXT NOT NULL,
      family TEXT NOT NULL,
      name TEXT NOT NULL,
      name_bn TEXT NOT NULL,
      description TEXT NOT NULL DEFAULT '',
      description_bn TEXT NOT NULL DEFAULT '',
      emoji TEXT NOT NULL DEFAULT '📝',
      total_questions INTEGER NOT NULL DEFAULT 20,
      duration_seconds INTEGER NOT NULL DEFAULT 1200,
      mark_per_question REAL NOT NULL DEFAULT 1,
      negative_mark_per_wrong REAL NOT NULL DEFAULT 0,
      shuffle_questions INTEGER NOT NULL DEFAULT 1,
      shuffle_options INTEGER NOT NULL DEFAULT 1,
      sections_json TEXT NOT NULL DEFAULT '[]',
      years TEXT NOT NULL DEFAULT '[]',
      content_version INTEGER NOT NULL DEFAULT 1,
      pack_id TEXT
    )`,
    `CREATE INDEX IF NOT EXISTS idx_exams_family ON exams (family)`,

    `CREATE TABLE IF NOT EXISTS exam_attempts (
      id TEXT PRIMARY KEY,
      exam_id TEXT NOT NULL,
      started_at INTEGER NOT NULL,
      finished_at INTEGER,
      score REAL NOT NULL DEFAULT 0,
      max_score REAL NOT NULL DEFAULT 0,
      accuracy REAL NOT NULL DEFAULT 0,
      total_questions INTEGER NOT NULL DEFAULT 0,
      correct INTEGER NOT NULL DEFAULT 0,
      wrong INTEGER NOT NULL DEFAULT 0,
      skipped INTEGER NOT NULL DEFAULT 0,
      total_time_ms INTEGER NOT NULL DEFAULT 0,
      result_json TEXT NOT NULL DEFAULT '{}',
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL,
      deleted_at INTEGER,
      sync_status TEXT NOT NULL DEFAULT 'local',
      last_synced_at INTEGER,
      version INTEGER NOT NULL DEFAULT 1
    )`,
    `CREATE INDEX IF NOT EXISTS idx_exam_attempts_exam ON exam_attempts (exam_id, started_at DESC)`,

    `CREATE TABLE IF NOT EXISTS games (
      id TEXT PRIMARY KEY,
      kind TEXT NOT NULL UNIQUE,
      name TEXT NOT NULL,
      name_bn TEXT NOT NULL,
      description TEXT NOT NULL DEFAULT '',
      description_bn TEXT NOT NULL DEFAULT '',
      emoji TEXT NOT NULL DEFAULT '🎮',
      duration_seconds INTEGER NOT NULL DEFAULT 60,
      generator_ids TEXT NOT NULL DEFAULT '[]',
      brain_category TEXT NOT NULL DEFAULT 'mental_math',
      start_difficulty INTEGER NOT NULL DEFAULT 2,
      difficulty_step_up REAL NOT NULL DEFAULT 1,
      difficulty_step_down REAL NOT NULL DEFAULT 1,
      lives_allowed INTEGER NOT NULL DEFAULT 3,
      content_version INTEGER NOT NULL DEFAULT 1,
      pack_id TEXT
    )`,

    `CREATE TABLE IF NOT EXISTS game_scores (
      id TEXT PRIMARY KEY,
      game_kind TEXT NOT NULL,
      score INTEGER NOT NULL DEFAULT 0,
      correct INTEGER NOT NULL DEFAULT 0,
      wrong INTEGER NOT NULL DEFAULT 0,
      max_combo INTEGER NOT NULL DEFAULT 0,
      duration_ms INTEGER NOT NULL DEFAULT 0,
      played_at INTEGER NOT NULL,
      difficulty_reached INTEGER NOT NULL DEFAULT 1,
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL,
      deleted_at INTEGER,
      sync_status TEXT NOT NULL DEFAULT 'local',
      last_synced_at INTEGER,
      version INTEGER NOT NULL DEFAULT 1
    )`,
    `CREATE INDEX IF NOT EXISTS idx_game_scores_kind ON game_scores (game_kind, score DESC)`,

    `CREATE TABLE IF NOT EXISTS challenges (
      id TEXT PRIMARY KEY,
      kind TEXT NOT NULL,
      name TEXT NOT NULL,
      name_bn TEXT NOT NULL,
      description TEXT NOT NULL DEFAULT '',
      description_bn TEXT NOT NULL DEFAULT '',
      emoji TEXT NOT NULL DEFAULT '🔥',
      topic_id TEXT,
      question_count INTEGER NOT NULL DEFAULT 10,
      duration_seconds INTEGER NOT NULL DEFAULT 600,
      hints_allowed INTEGER NOT NULL DEFAULT 1,
      difficulty_start INTEGER NOT NULL DEFAULT 2,
      difficulty_end INTEGER NOT NULL DEFAULT 7,
      xp_reward INTEGER NOT NULL DEFAULT 100,
      coin_reward INTEGER NOT NULL DEFAULT 50,
      required_mastery REAL NOT NULL DEFAULT 0,
      content_version INTEGER NOT NULL DEFAULT 1,
      pack_id TEXT
    )`,
    `CREATE INDEX IF NOT EXISTS idx_challenges_kind ON challenges (kind)`,
    `CREATE INDEX IF NOT EXISTS idx_challenges_topic ON challenges (topic_id)`,

    `CREATE TABLE IF NOT EXISTS challenge_attempts (
      id TEXT PRIMARY KEY,
      challenge_id TEXT NOT NULL,
      started_at INTEGER NOT NULL,
      finished_at INTEGER,
      score INTEGER NOT NULL DEFAULT 0,
      correct INTEGER NOT NULL DEFAULT 0,
      total INTEGER NOT NULL DEFAULT 0,
      passed INTEGER NOT NULL DEFAULT 0,
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL,
      deleted_at INTEGER,
      sync_status TEXT NOT NULL DEFAULT 'local',
      last_synced_at INTEGER,
      version INTEGER NOT NULL DEFAULT 1
    )`,
    `CREATE INDEX IF NOT EXISTS idx_challenge_attempts_challenge ON challenge_attempts (challenge_id, started_at DESC)`,

    `CREATE TABLE IF NOT EXISTS missions (
      id TEXT PRIMARY KEY,
      code TEXT NOT NULL,
      period_key TEXT NOT NULL,
      target INTEGER NOT NULL DEFAULT 1,
      progress INTEGER NOT NULL DEFAULT 0,
      completed INTEGER NOT NULL DEFAULT 0,
      claimed_at INTEGER,
      created_at INTEGER NOT NULL,
      updated_at INTEGER NOT NULL,
      deleted_at INTEGER,
      sync_status TEXT NOT NULL DEFAULT 'local',
      last_synced_at INTEGER,
      version INTEGER NOT NULL DEFAULT 1
    )`,
    `CREATE UNIQUE INDEX IF NOT EXISTS idx_missions_unique ON missions (code, period_key)`,
  ],
};
